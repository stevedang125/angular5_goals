// **************** no long work for Angular 5 ************


// 3. Setting up route(main.ts, Bootstrap[])
//   Step 1: Create a routes file, and export these routes so they can be loaded into main.ts file
//   Step 2: Tell Angular where to load these route in our components' views when a route is requested


// import { DirectoryComponent } from './directory/directory.component';
// import { HomeComponent } from './home/home.component';
// import { RouterModule, Routes } from '@angular/router';
// // ************ no longer used:(from angular 5+)
// //************** import { provideRouter } from '@angular/router';
//
// const appRoutes: Routes = [
//   { path: 'directory', component: DirectoryComponent },
//   { path: '', component: HomeComponent }
// ];
//
//
// export const APP_ROUTERS_PROVIDER = [
//   provideRouter(appRoutes)
// ];
